# Example 10: Tuples
# Topics: tuple immutability, unpacking
point = (3, 4)
x, y = point
print("x=", x, "y=", y)
# Tuples are immutable: the next line would raise an error if uncommented
# point[0] = 5

# Task: swap two variables using tuple unpacking
