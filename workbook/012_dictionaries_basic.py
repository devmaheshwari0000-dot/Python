# Example 12: Dictionaries
# Topics: key-value pairs, get(), keys(), items()
d = {"name": "Eve", "age": 30}
print(d.get("name"))
for k, v in d.items():
    print(k, v)

# Task: count character occurrences in a string using a dict
