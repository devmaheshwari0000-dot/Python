# Example 61: Simple HTTP request with requests
# Topics: requests.get
# Requires 'requests' package
import requests
# r = requests.get('https://httpbin.org/get')
# print(r.status_code, r.json())

# Task: perform a POST request with JSON payload (example only)
