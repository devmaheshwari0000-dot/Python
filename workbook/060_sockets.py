# Example 60: Sockets (simple TCP client)
# Topics: socket
import socket
# This is example code - do not run in restricted environments
# client = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
# client.connect(('example.com', 80))

# Task: write a simple TCP server that accepts one connection and echoes data
